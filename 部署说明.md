# 部署说明

本站为**纯静态网站**（HTML + CSS + 原生 JavaScript），无后端、无数据库、无构建步骤，把整个文件夹上传到任意静态托管服务即可上线。

## 多语言结构

本站包含 7 种语言版本，共 49 个页面：

| 语言 | 目录 | hreflang |
|---|---|---|
| 英语（默认） | `/` 根目录 | `en` |
| 中文 | `/zh/` | `zh-CN` |
| 西班牙语 | `/es/` | `es` |
| 阿拉伯语（RTL） | `/ar/` | `ar` |
| 俄语 | `/ru/` | `ru` |
| 日语 | `/ja/` | `ja` |
| 韩语 | `/ko/` | `ko` |

每个页面都带有完整的 hreflang 互链标签（含 `x-default` 指向英文版），sitemap.xml 也已包含全部语言 URL 及 alternate 声明。页面右上角有语言切换器。部署时**保持目录结构不变**即可。

**旧 `/en/` 路径重定向**：默认语言从中文改为英文后，原 `/en/` 目录已移除。站点根目录附带 `vercel.json`（Vercel 用）和 `_redirects`（Cloudflare Pages 用）两个配置文件，会把旧的 `/en/`、`/en/games/xxx/` 链接 301 重定向到根目录对应页面，部署时随站点一起上传即可生效，无需额外操作。

如需修改文案或新增语言：编辑 `tools/build_i18n.py` 中的翻译数据，然后重新运行 `python3 tools/build_i18n.py` 即可重新生成全部页面（注意：会覆盖同名 HTML 文件）。`tools/` 目录不参与网站运行，部署时可删除。

## 一、部署前必做：替换域名

全站 SEO 标签、canonical、sitemap 中的占位域名为 `https://games.example.com`，部署前请全局替换为你的真实域名：

```bash
# 在网站根目录执行（macOS / Linux）
grep -rl "games.example.com" . | xargs sed -i "s|games.example.com|你的域名|g"
```

涉及文件：`index.html`、`games/*/index.html`、`sitemap.xml`、`robots.txt`。

## 二、部署方式（任选其一）

### 1. 对象存储 / CDN（推荐国内访问）
- **阿里云 OSS / 腾讯云 COS**：新建 Bucket → 开启静态网站托管 → 上传全部文件 → 设置默认首页 `index.html`、404 页面 `404.html` → 绑定域名并开启 HTTPS。
- 配合 CDN 加速，并在 CDN 上提交 sitemap 加快收录。

### 2. Vercel / Netlify（免费、自动 HTTPS）
- 把整个文件夹拖入 Vercel/Netlify 的部署页面即可，或关联 Git 仓库自动部署。
- 无需任何构建配置（Build Command 留空，输出目录为根目录）。

### 3. GitHub Pages（免费）
- 将文件推送到仓库，Settings → Pages → 选择分支根目录，即可通过 `username.github.io` 访问。

### 4. 自己的服务器（Nginx）
```nginx
server {
    listen 80;
    server_name 你的域名;
    root /var/www/games;   # 文件上传到此目录
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;

    # 静态资源缓存，提升加载速度（SEO 加分项）
    location ~* \.(css|js|svg|png|jpg|woff2)$ {
        expires 30d;
        add_header Cache-Control "public";
    }

    # 开启 gzip
    gzip on;
    gzip_types text/css application/javascript text/html application/xml;
}
```

## 三、上线后 SEO 操作清单

1. **确认 HTTPS 已开启**（搜索引擎排名因素之一）。
2. **提交 Sitemap**：
   - 百度站长平台：https://ziyuan.baidu.com → 站点管理 → 添加站点 → 提交 `https://你的域名/sitemap.xml`
   - Google Search Console：https://search.google.com/search-console → 同样提交 sitemap
3. **验证结构化数据**：用 Google 富媒体测试（https://search.google.com/test/rich-results）检查首页和游戏页的 JSON-LD。
4. **生成 og-cover.png**：首页 OG 标签引用了 `/assets/og-cover.png`（建议 1200×630），放一张网站封面图用于社交分享预览；不需要时可删除对应 meta 行。

## 四、日常维护

- **新增游戏**：复制 `games/snake/` 目录为模板，修改其中的标题、meta、JSON-LD 和游戏 JS，并在首页游戏网格、页脚链接和 `sitemap.xml` 中各加一条记录。
- **更新 sitemap 的 lastmod**：每次内容更新后顺手改一下日期，有助于搜索引擎重新抓取。
